# 更新日志

每个发布版本改了什么记在这里。README 只讲怎么用，不再夹带版本变化。

版本号同时写在 `manifest.json` 的 `version` 字段里，扩展自己也是从这个字段读版本。

## 0.3.4 — 2026-09-14

### 修复

- **资料编辑器的「家庭背景」没有图标。** 导航标签与导航图标是两张手写的表，`family` 那一项从来没
  写进图标表，那一行只剩文字。现在补上图标，并给校验脚本与冒烟测试各加一道检查：任何一个导航项
  画不出图形都会直接报错。
- **「网站记忆」写草稿时可能覆盖掉你刚在弹窗或资料编辑器里保存的内容。** 草稿由页面脚本写入，资料
  由另外两个页面写入，三边都在读改写同一份资料；页面脚本因为单独注入、拿不到共用的合并逻辑，原来
  是直接整份写回。现在写完会回读核对，发现草稿被别处的写入盖掉就在新资料上重写一次，别处的改动不会
  再被旧副本冲掉。

### 变更

- **扫描结果缓存不再保存证件号码这类敏感值。** 这类栏目的值本来就在资料里，缓存再存一份等于同一串
  数字在本地出现两处。现在缓存只记「哪一栏是什么」，重开弹窗恢复时从资料读回；你自己在那一行敲进去
  的值不受影响，仍然会保留。
- 版本号的唯一来源是 `manifest.json`。校验脚本改为拿它与 `CHANGELOG.md`、`README.md` 里的版本比对，
  不再在脚本里另写一份，发版时漏改一处会被直接拦下。
- 清理 `content.js` 里被后续版本取代的重复注释，以及判断恒真的一个条件。
- 删掉仓库里已无脚本引用的 `tmp/` 临时目录（测试产物统一在 `测试/输出/`）。
- `tools/make_preview_images.ps1` 修好在 Windows PowerShell 5.1 下运行：Chromium 往 stderr 写
  「多少字节已写入」，5.1 会把它当成终止性错误，截完第一张图就中断。现在截图与渲染检查两处调用
  局部放宽 `ErrorActionPreference`，5.1 与 PowerShell 7 下都能跑完三张图。

## 0.3.3 — 2026-09-14

### 修复

- **无名标签的网申页上，下拉框 / 单选组 / 级联选择器填不进去。** 中国兵器工业集团招聘网这类页面不给控件任何
  `name` / `id` / `aria-label`，字段名只写在容器里的 `<label>`
  （`div.form-item--phoenix > .form-item__title > label.form-item__text`）。这些控件被当成「没有名字的字段」
  直接丢掉，压根不出现在弹窗的字段列表里。现在这类版面能正常识别。

### 新增

- `div.phoenix-select` 这种自定义下拉：选项在 `.phoenix-selectList__listItem` 里，没有 `role="option"`。
- **连 `<input>` 都没有的纯 div 单选组**（性别这类），选中态记在 `--checked` 类上。
- 省 → 市 →「确定」的**两级级联**地区选择器。匹配只按文字走，所以不依赖页面用哪家组件库；资料里写
  `辽宁-锦州-黑山县`、页面显示 `辽宁省/锦州市` 也能对上。级联的「确定」只会点弹出面板里的那一个，
  不会碰到页面自己的「保存」按钮。

## 0.3.2 — 2026-09-14

### 修复

- 自定义下拉（例如「婚姻状况」）点了「帮我填上」却没有真正写入。现在会逐个尝试点开下拉、按选项文本点击，
  再回读控件确认页面状态确实变了。
- 单选组（例如「性别」「有无子女」）填不上：这类控件的真实 `<input>` 常被做成透明层，之前会被当成不可见
  直接跳过；而且直接赋值不会触发 React/Vue 的状态更新。现在改为扫描时放行、填充时用真实点击。
- 文本栏写入后会核对页面是否真的接受了这个值，不再谎报成功。
- 测试工具链在 Windows PowerShell 5.1 下的三个问题：读 UTF-8 文件乱码、打出的 zip 用反斜杠当目录分隔符、
  中文文件名经管道传给 node 会变成问号。现在 5.1 与 PowerShell 7 下都是全绿。

### 新增

- 弹窗被点到页面其他地方关闭后，重新打开会**自动恢复上一次的扫描结果**，不必重新扫描。（弹窗被点走就关闭
  是 Chrome 的固定行为，扩展改不了；能改的是不再丢结果。）
- 扫描与填充可以进入 **open shadow root** 与 **contenteditable** 控件。

### 变更

- 弹窗与资料编辑器共用的存储合并逻辑收进 `shared.js`：原先两份逐字重复的实现合成一份，读写失败现在会
  明确报错而不是静默失败。

## 0.3.0 — 2026-09-13

### 新增

- 弹窗改版：分成「智能填写」与「资料速查」两个标签页，扫描结果可按 全部 / 已对上 / 待确认 / 未匹配 筛选，
  支持批量选择与「仅选已匹配」，并能在页面上高亮定位某一栏。
- 开始按真实站点校订栏目措辞：抓取件与派生词表放在 `测试/抓取/`，`测试/sites-fixture.html` 收拢了这些
  站点出现过的栏目文案，`sites-smoke.ps1` 把它变成回归基线。

### 变更

- `.gitignore` 补齐个人文件、导出文件与测试临时产物的忽略规则。

## 0.2.0 — 2026-09-11

首个公开版本。

- 模板文本与资料对象之间的解析、序列化与导出（`profile-parser.js`）。
- 页面扫描与匹配：别名表、按栏目容器取名字、置信度分级。
- 填充：文本框、文本域、原生下拉、单选、复选、自定义下拉、日期拆分控件。
- 弹窗与资料编辑器两套界面，WinUI 3 设计令牌。
- 本地提示：密码、验证码、身份证、银行卡、安全码和文件选择字段不保存、不填。
- 图标、扩展 key 固定（更新不丢资料）与打包脚本。
